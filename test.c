#include <stdio.h>
#include "io.h"
#include "first-task.h"

int main(void){

    // FILE * test = fopen("task-one-test.in", "r");
    // printf("%d\n", read_seed(test));

    //generate_sequence_of_pieces(231, 20);
    //read_and_respond_to_all_input_files();
    return 0;
}
